# DevLink Frontend

This is a Vite + React + TypeScript starter for the DevLink frontend.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The frontend expects an API at `/api` by default. Configure `VITE_API_BASE` to change.
