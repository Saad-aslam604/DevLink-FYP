import React from 'react'

// TestBookingButton removed — left as no-op stub to avoid import errors.
// If you want this file deleted entirely from the repo, I can delete it; for now it will render nothing.
export default function TestBookingButton() {
  return null
}
