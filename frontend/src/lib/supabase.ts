// Supabase client removed — frontend now calls the local Express API.
// This file remains as a placeholder to avoid accidental imports. Importing
// `supabase` will throw to make the migration obvious.
export const supabase = (() => {
  throw new Error('Supabase client removed from frontend. Use REST API endpoints under /api/* instead.')
})()
